# Pull Request Template

GitHub uses `.github/PULL_REQUEST_TEMPLATE.md` when a contributor opens a pull request.

Keep the summary focused on behavior changed, not every file touched. Mark only validation commands that were actually run, and use Notes for deferred work, failed checks, or review context.
